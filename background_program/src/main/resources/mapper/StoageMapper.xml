<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.background_program.mapper.StoageMapper">
    <select id="pageC" resultType="com.example.background_program.entity.Stoage">
        select * from stoage
    </select>
    <select id="pageCC" resultType="com.example.background_program.entity.Stoage">
        select * from stoage ${ew.customSqlSegment}
    </select>
</mapper>
